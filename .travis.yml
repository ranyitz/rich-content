language: node_js
node_js:
  - 10
install:
  - npm install
jobs:
  include:
    - stage: 'Build & Test'
      script:
        - npm run build
        - npm test
    - stage: 'Publish'
      script:
        - npm run customPublish
